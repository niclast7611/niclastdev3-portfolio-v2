import React from "react";

type Props = {};

export const Footer = (props: Props) => {
  return (
    <footer className="relative bottom-0 w-full h-24 bg-blue-500">
      Footer
    </footer>
  );
};
